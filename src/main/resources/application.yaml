spring:
  application:
    name: Wunschzettel
  datasource:
    url: jdbc:postgresql://localhost:5432/wunschzettel
    username: admin
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
